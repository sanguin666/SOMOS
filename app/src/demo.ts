// Fixed QR token for the demo POI seeded by `backend/src/seed.ts`
// (run `npm run seed` in backend/). Stands in for a real camera scan
// until QR scanning and multi-place support are built.
export const DEMO_QR_TOKEN = 'DEMO-STMARYS';
