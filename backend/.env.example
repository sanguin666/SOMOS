# Copy this file to .env for local development.
NODE_ENV=development
PORT=3000

# Must match the docker-compose.yml at the root of the repo.
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=mypeople
DB_PASSWORD=mypeople
DB_NAME=mypeople

# Signs the admin dashboard's login tokens. The fallback in
# src/auth/auth.module.ts is fine for the local demo only — set a real
# secret before any production use.
# JWT_SECRET=
