# Copy this file to .env for local development.
NODE_ENV=development
PORT=3000

# Must match the docker-compose.yml at the root of the repo.
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=mychurch
DB_PASSWORD=mychurch
DB_NAME=mychurch
