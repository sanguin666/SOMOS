/**
 * Product modules a church can subscribe to.
 * Add new modules here as the product grows (e.g. messages, classes, etc.).
 */
export enum ModuleType {
  DONATIONS = 'donations',
  EVENTS = 'events',
}
