/**
 * Modules produit disponibles à la souscription pour un lieu de culte.
 * Ajouter ici les futurs modules (ex: messages, cours, etc.).
 */
export enum TypeModule {
  DONS = 'dons',
  EVENEMENTS = 'evenements',
}
