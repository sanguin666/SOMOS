/**
 * The kind of point of interest, used to pick a visual theme in the app
 * (accent color, imagery, wording). Church is the only type built out so
 * far; the product may extend to other kinds of venues/organizations later
 * (the brief mentions "milieu associatif au sens large").
 */
export enum PoiType {
  CHURCH = 'church',
}
