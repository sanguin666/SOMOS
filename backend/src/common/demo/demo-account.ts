/**
 * The demo's member account: signing in to the app goes straight into it,
 * with no code by text message, so a demo on a phone takes one tap (Seb,
 * 24 Sep 2026). The seed creates it as a member of Nuestra Señora del
 * Carmen. Only while the backend has no SMS sender and is not running in
 * production; DEMO_LOGIN=off turns it off anyway.
 */
export const DEMO_MEMBER_PHONE = '+34600000001';
