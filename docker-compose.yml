services:
  postgres:
    image: postgres:16-alpine
    container_name: mychurch-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: mychurch
      POSTGRES_PASSWORD: mychurch
      POSTGRES_DB: mychurch
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
