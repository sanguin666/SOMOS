services:
  postgres:
    image: postgres:16-alpine
    container_name: ansae-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: ansae
      POSTGRES_PASSWORD: ansae
      POSTGRES_DB: ansae
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
